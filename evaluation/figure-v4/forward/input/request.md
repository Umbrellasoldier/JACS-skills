Use $jacs-figure from the supplied snapshot to create a double-column figure from predictions.csv and context.json. The figure should help a chemistry reader understand how the two models differ in bias and error size and whether their performance varies over the barrier range. Choose the panels yourself. Deliver PDF, SVG, PNG, a reader-facing caption, rerunnable source, and concise verification notes. These are synthetic data. Use only the supplied input and local skill snapshot; write all outputs to the output directory. Do not publish or modify the repository or installed skills. Environment: the Python environment locked in this repository (path normalized for publication).
